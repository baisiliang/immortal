---
description: 本项目的 AI 交互记录规则——设计/头脑风暴的关键决策必须留档、归类整理到 agent交互/ 目录，方便后续查阅并避免全量上下文
alwaysApply: true
---

# AI 交互记录规则

与用户进行**游戏设计 / 头脑风暴 / 方案决策**类交互时，关键结论必须**记录留档**，集中归档到项目根目录的 `agent交互/` 下。

## 目的

- 后续交互**只需读取相关主题的记录**，无需把全部历史对话传入上下文。
- 决策可追溯、可查阅。

## 归档规则

1. **位置**：所有交互记录放在 `agent交互/`，**按主题分子文件夹**归类，命名 `序号_主题/`（如 `04_功法武学/`）。
2. **每个主题**至少有一份 `决策记录.md`，包含：
   - **已定**：拍板的结论（含必要的规则 / 数值 / 取舍）。
   - **待定**：尚未决定、留待后续的问题。
   - 必要时附**讨论备注**（被否决的方案及原因，避免重复讨论）。
3. **索引**：`agent交互/README.md` 维护子文件夹与对应策划文档的对照表、总进度、"下次继续"的接续点；每完成一个模块都要更新。
4. **时机**：每个模块 / 主题澄清完成（或当轮交互产出重要决策）后**即时记录**，不要堆积。
5. **粒度**：只记**关键决策与结论**，不誊录完整对话；力求简洁、便于查阅。

## 与策划文档的分工

- `策划/` 下是**正式设计文档**（面向"游戏是什么"）。
- `agent交互/` 下是**决策过程记录**（面向"为什么这么定、还差什么"）。
- 两者交叉引用，但不互相重复大段内容。

## 其他约束

- 记录文档同样遵守项目约束：**不出现其他游戏名 / 其他游戏的内容**。
